<head></head>

<body>
  <div>
    <button onclick="testProxy()">Proxy</button>
  </div>
</body>

<script>
  function testProxy() {
    const url = 'https://sandbox-plus-sdi-y1-development.myshopify.com/apps/orders-rating';
  
    return new Promise((resolve, reject) => {
      fetch(`${url}`, {
        method: 'POST',
        redirect: 'manual',
        headers: {
          'Content-Type': 'application/json',
          'Access-Control-Allow-Origin': '*',
        },
        // Add body if required by the server:
        // body: JSON.stringify({ /* data to send */ }),
      })
        .then((response) => {
          console.log(response, 'response');
          return response.json();
        })
        .then((data) => {
          resolve(data, 'data');
        })
        .catch((error) => {
          reject(error);
        });
    });
  }
  
</script>

{% schema %}
{
  "name": "Star Rating XXL",
  "target": "section",
  "settings": [
    { "type": "product", "id": "product", "label": "product", "autofill": true },
    { "type": "color", "id": "colour", "label": "Star Colour", "default": "#ff0000" }
  ]
}
{% endschema %}

